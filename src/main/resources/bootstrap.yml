encrypt:
  key: ABCDEFGHIJKLMNOPQRSTUVWXYZ

spring:
  profiles:
    active: prod
  cloud:
    config:
      username: arpit
      password: bhargava
    discovery:
      enabled: true
  application:
    name: config-client-app

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

security:
  basic:
    enabled: true